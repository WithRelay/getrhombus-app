current stripe api version - 2014-03-28
https://www.getrhombus.com/receive_delivery_report

Bhishma
- do we need the application controller code for messenger
- Plans/Subscriptions
	- create subscription api modal for customer profile/messaging page...
	- Add logic for switching plans/subscriptions
		- page can reload
	- In plans, remove user_id, add merchant_id and customer_id to track customer plans
	- Add back cancel subscription now
- if response['success'] || fb_pages.empty? || FbPage.where(subscription_status: true).empty?
- using page_id alone?

surya
- can I migrate my jobs?
- Campaigns
	- use custom method to get campaign users for all channels - it now return array of hashes of user objects
	- move send messenger campaign code from services. You might need to restructure and rename your classes..so things are clearer. 
	- Implement SMS/MMS channel. They are ready. 
	- wipe subject if campaign is not email - serverside only is fine
	- check for date on submit
- Upgrade Trumbowyg
	- so we can use new ordered emojis (generate unicode in the same order in the emoji plugin file)
	- so we can get the scroll bar fix - i raised an issue here - https://github.com/Alex-D/Trumbowyg/issues/458
	- add color plugins
- Implement Reminders
	- Like campaigns but uses api to create reminder - route should be restful
	- Reminders index, edit, update and destroy are needed. You can use the campaign controller for all these, but only reminder type campaigns show up in the index...the reverse is true for campaigns. And reminders has a reminder path. http://rhombus-test.webflow.io/messaging-dashboard ...click on bell icon
- Managed Account 
	- break up form - http://thecodeplayer.com/walkthrough/jquery-multi-step-form-with-progress-bar
- Add plugin versions to file
- make sure name in campaigns cannot be an empty string

Ovo
- add to list from user profile - http://rhombus-test.webflow.io/customer-profile
- shouldn't be able to edit default segments

Edwin
- create pages to show lists (with edit and delete links) of reminders and edit a reminder - no modal
- generate page loader

Taiwo (review)
- review stripe_events
- review merchant_customer_id in transactions and invoices
- review managed account..
- subscription, subscription controller and stripe customer creation places
- Review adding token...failing safely.
- on subscription - check if user alreaokdy belongs to merchant??? is this when they text?
	- customers can create subscriptions over messaging...not dashboard, but can cancel subscription
- finish message parser and review how messsages are saved
- final review of updating subscriptions, coupons, plans...do we update subscriptions esp platform??
- Rake - migrate customer uri to merchant_customer plus drop column...but after migrating merchant_customer
   - remove livemode from users, add to merchant_customer and create rake task to migrate this to merchant..customer

Review
- campaign_user_list (can better track failures) or notification_log or add an email model??
- notification_log used for transaction, referrer and alert notifications logging
- remove first_name and last_name assignment in angular code
- when user logs in through messenger, indicate in referrers
- change all emails in test db dump
- unify team and dashboard email issue really
- indexes for conversations table later
- create Seeds
- notifications platform to merchants should have a single conversation and should not be pulled - mark as read too
- conversations....need origin...cos system to customers should not open conversation....
- return omniauth stripe connect for dashboard account - check how we save platform stripe cred...does stripe connect still work? do we really need this?

Payment Capture - should be for only customers....capture in logic
- check if /profile affect forms for merchants
- plug in charge into payment capture workflow
- let parse_text return cents i guess and just divide by 100 to show

Contact - Inbound
- sent u a text doesnt have an account 
- have an account, sent u a text but ref link isnt yours 


http://rhombus-test.webflow.io/account-settings
http://rhombus-test.webflow.io/business-settings
http://rhombus-test.webflow.io/setup-payments
http://rhombus-test.webflow.io/bank-account

