<h1>Listing Coupons</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @coupons.each do |coupon| %>
      <tr>
        <td><%= coupon.name %></td>
        <td><%= "%.2f" %(coupon.amount_off.to_f) %></td>
        <td><%= coupon.currency %></td>
        <td><%= coupon.duration %></td>
        <td><%= coupon.duration_in_months %></td>
        <td><%= coupon.max_redemptions %></td>
        <td><%= coupon.percent_off %></td>
        <td><%= coupon.redeem_by %></td>
        <td>
          <% if coupon.subscription_id.blank? %> 
            <%= link_to 'Destroy', user_coupon_path(current_user, coupon), method: :delete, class: "delete-coupon coupon#{coupon.id}" %></td>
          <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Coupon', new_user_coupon_path %>

<br><br><br>



 <%#= render :partial => '/referrers/form' %>
