<div class="container">
  <h1>Listing Subscriptions</h1>

  <table class="table">
    <thead>
      <tr>
        <th>Customer</th>
        <th>Status</th>
        <th>Plan</th>
        <th>Discount</th>
        <th>Quantity</th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <% @subscriptions.each do |subscription| %>
        <tr>
          <td><%= present(subscription).get_customer_info %></td>
          <td><%= present(subscription).status %></td>
          <td><%= present(subscription).get_plan_info %></td>
          <td><%= present(subscription).get_coupon_info %></td>
          <td><%= subscription.quantity %></td>
          <!-- <td><%#= link_to 'Show', subscription %></td> -->
          <!-- <td><%#= link_to 'Edit', edit_subscription_path(subscription) %></td> -->
          <td>
            <% if subscription.cancel_at_period_end  %>
              <span class="label label-info">Cancel at period end</span>
            <% elsif subscription.status != 'canceled' %>
              <%= link_to 'Cancel Subscription', user_subscription_path(current_user, subscription), method: :delete, class: "btn btn-danger cancel-subscription subscription#{subscription.id}", data: { disable_with: "<i class='fa fa-spinner fa-spin'></i>Please wait...".html_safe } %>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%#= link_to 'New Subscription', new_subscription_path %>
  <%= link_to 'Create Subscription', new_user_subscription_path, id: 'create-subscription' %>


</div>