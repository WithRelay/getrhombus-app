<div class="alt help-center hero" data-ix="show-fixed-navbar">
  <div class="help-center hero-content w-container">
    <div class="categories relay-docs-logo-wrapper w-row">
      <div class="w-col w-col-6">
        <%= link_to root_url do%>
            <%= image_tag('Relay-Transparent-White.png', class: 'relay-logo-docs', width: 67) %>
        <%end%>
        <div class="docs-page-title">
         <%if controller.action_name == "terms" %>
            Terms of Use
          <%elsif controller.action_name == 'privacy' %>
          Privacy Policy
          <% else %>
          Help Center
          <%end%>
        </div>
      </div>
      <div class="relay-docs-logo-wrapper-column-2 w-col w-col-6">
        <% if user_signed_in? %>
          <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: 'sign-in-link' %>
        <%else%>
          <%= link_to 'Sign in', new_user_session_url, class: 'sign-in-link'%>
        <%end%>
      </div>
    </div>
  </div>
</div>
