<!-- <div class="customer-dashboard side-bar w-clearfix"> -->
  <div class="dashboard-logo-wrapper">
    <%= link_to user_path(current_user) do %>
      <%= image_tag('Relay-Transparent.png', width: 50, class: 'dashboard-logo') %>
    <% end %>
  </div>

  <h5 class="left-nav signup-box-subheading shrink-text"><%= sidebar_name "#{params[:controller]}-#{params[:action]}" %></h5>

  <% if current_user.is_merchant? %>

    <%= link_to user_campaigns_path(current_user), class: 'left-nav-tab-wrapper w-inline-block' do %>
      <div class="left-nav-tab">Campaigns</div>
    <% end %>

    <%= link_to user_merchant_contacts_path(current_user), class: "left-nav-tab-wrapper w-inline-block" do %>
      <div class="left-nav-tab">Contacts &nbsp; &nbsp;</div>
    <% end %>

    <div class="customers-hidden-menu">
      <div>
        <select class="dropdown segments w-select" data-name="Segment Select" id="contacts-segment-list" name="Segment-Select">
          <option value="">Contacts Segments</option>
          <% current_user.segments.contact.each do |s| %>
            <option value="<%= s.id %>" <%= "selected" if s.id == @selected_segment_id %> ><%= s.name %></option>
          <% end %>
        </select>
      </div>
    </div>

    <%= link_to user_merchant_customers_path(current_user), class: "left-nav-tab-wrapper w-inline-block" do %>
      <div class="left-nav-tab">Customers &nbsp; &nbsp;</div>
    <% end %>

    <div class="customers-hidden-menu">
      <div>
        <select class="dropdown segments w-select" data-name="Segment Select" id="Segment-Select-lists" name="Segment-Select">
          <option value="">Customers Segments</option>
          <% current_user.segments.customer.each do |s| %>
            <option value="<%= s.id %>" <%= "selected" if s.id == @selected_segment_id %> ><%= s.name %></option>
          <% end %>
        </select>
      </div>
    </div>

    <%= link_to user_hashtags_path(current_user),  class: "left-nav-tab-wrapper w-inline-block" do %>
      <div class="left-nav-tab">Hashtags</div>
    <% end %>

    <%= link_to user_lists_path(current_user), class: 'left-nav-tab-wrapper w-inline-block' do %>
      <div class="left-nav-tab" data-ix="display-and-hide-customers">Lists</div>
    <% end %>

    <a class="left-nav-tab-wrapper w-inline-block" href="#">
      <div class="left-nav-tab messaging" id="display-and-hide-left-nav">Messaging &nbsp; &nbsp; <span class="sidebar-dropdown-icon">+</span>
      </div>
    </a>

    <div class="messaging-hidden-menu hide">
      <%= link_to 'Conversations', user_conversations_path(current_user), class: 'conversations dashboard-dropdown-link' %>
      <%= link_to 'Saved replies', user_saved_replies_path(current_user), class: 'dashboard-dropdown-link saved-rep' %>
      <%= link_to 'Scheduled messages', user_reminders_path(current_user), class: 'dashboard-dropdown-link scheduled-msgs' %>
    </div>

  <% elsif current_user.is_customer? %>

    <%= link_to user_organizations_path(current_user), class: "left-nav-tab-wrapper w-inline-block" do %>
      <div class="left-nav-tab">Organizations</div>
    <% end %>

  <% end %>

  <a class="left-nav-tab-wrapper w-inline-block" href="#" >
    <div class="left-nav-tab transactions" id="display-and-hide-transaction-tab">Transactions&nbsp;&nbsp; &nbsp;<span class="sidebar-dropdown-icon">+</span>
    </div>
  </a>

  <div class="transactions-hidden-nav hide">
    <%= link_to 'Charges', user_transactions_path(current_user), class: 'charges dashboard-dropdown-link' %>
    <%= link_to 'Pre-authorized', user_transactions_path(current_user, captured: false), class: 'preauth dashboard-dropdown-link' %>
    <%= link_to 'Subscriptions', user_subscriptions_path(current_user ), class: 'subscription dashboard-dropdown-link' %>
    <% if  current_user.is_platform? %>
      <%= link_to 'Coupons', user_coupons_path(current_user), class: 'subscription dashboard-dropdown-link' %>
    <% end %>
  </div>
