  <div class="dashboard-nav w-clearfix">
    <div class="dashboard-logo-wrapper">
      <%= link_to root_url do %>
        <%= image_tag('relay-6.png', class: 'dashboard-logo', width: 50) %>
      <% end %>
    </div>
    <div class="dashboard-nav-links-wrapper">
      <a class="w-clearfix w-inline-block">
        <%#=  Added class %>
        <div class="dash-header-links">
          <a href="<%= user_path(current_user) %>" class="header-links-fix"> &nbsp; <strong>Dashboard</strong></a>
        </div>
        <div class="dash-header-links new-link">
          <a href="<%= customers_user_path(current_user) %>" class="header-links-fix"> &nbsp;<strong>All &nbsp;customers</strong></a>
        </div>
        <div class="dash-header-links new-link">
          <a href="<%= contacts_user_path(current_user) %>" class="header-links-fix"> &nbsp;&nbsp;<strong>Contacts</strong></a>
        </div>
        <div class="dash-header-links new-link">
          <a href="<%= lists_path(current_user) %>" class="header-links-fix"> &nbsp;&nbsp;<strong>List</strong></a>
        </div>

          <%= render "shared/rhombus_number" %>

      </a>
    </div>

      <%= render 'shared/settings_dropdown' %>

  </div>
