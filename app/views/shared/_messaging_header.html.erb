  <div class="dashboard-nav messaging w-clearfix">
    
    <div class="dashboard-nav-links-wrapper">
      <div class="w-clearfix w-inline-block">
        <%#=  Added class %>
        <div class="dash-header-links">
          <a href="<%= user_path(current_user) %>" class="important-text-47 link_no_underline"> &nbsp; Dashboard</a>
        </div>
        <div class="dash-header-links new-link">
          <a href="<%= customers_user_path(current_user) %>" class="important-text-48 link_no_underline"> &nbsp;customers</a>
        </div>
        <div class="dash-header-links new-link">
          <a href="<%= user_reminders_path(current_user) %>" class="important-text-49 link_no_underline"> &nbsp;&nbsp;scheduled messages</a>
        </div>
        <div class="dash-header-links new-link">
          <a href="<%= lists_path(current_user) %>" class="important-text-50 link_no_underline"> &nbsp;&nbsp;Lists</a>
        </div>
        <div class="dash-header-links new-link">
          <a href="<%= user_campaigns_path(current_user) %>" class="important-text-50 link_no_underline"> &nbsp;&nbsp;Campaigns</a>
        </div>

        <%= render "shared/rhombus_number" %>

      </div>
    </div>

    <%= render 'shared/settings_dropdown' %>

  </div>
