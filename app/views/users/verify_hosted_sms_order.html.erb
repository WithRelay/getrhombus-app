 <% provide(:title, 'Relay - Verify Hosted Sms Order') %>

<div class="blank-page-content-wrapper">
  <div class="onboarding-form-div sign-in signup" style="padding-bottom: 85px;">
    <%= link_to root_url, class: 'form-logo w-inline-block' do %>
      <%= image_tag('Relay-Transparent-500x300.png', width: 50, class: 'password-reset-logo') %>
    <% end %>
    <div class="flex-underline form-header-underline"></div>
    <div class="password-reset-instructions">Enter your phone number verification code</div>
    <div class="log-in-form">
      <form method="post" action= <%= current_user.hosted_sms.try(:url) %> id="verify-hosted-sms-order" redirect_url= <%= user_sms_usage_path(current_user) %> >
        <input type="text" name="VerificationCode" placeholder="Verification code" autofocus="true" required="true" class="text-field w-input">
        <input type="submit" name="Verify" class="button log-in w-button" value="Verify & Continue" >
      </form>
    </div>
  </div>
</div>
