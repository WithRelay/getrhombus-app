<%= render partial: 'shared/profiles/profile', locals: {
                                                        customer: true,
                                                        user_snapshot: @user_snapshot,
                                                        transactions: @transactions,
                                                        conversation_refs: @conversation_refs,
                                                        recent_activity: @recent_activity
                                                        }%>
